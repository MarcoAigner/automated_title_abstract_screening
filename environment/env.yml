name: automated_screening
channels:
  - conda-forge
dependencies:
  - python
  - ipykernel # jupyter notebook kernel
  - pandas # dataframes
  - polars # alternative dataframe framework
  - pyarrow # required by polars
  - matplotlib # plotting
  - seaborn # statistical data visualization
  -  bs4 # BeautifulSoup for removing HTML tags
  - scikit-learn # models and pipelines
  - imbalanced-learn # pipelines that support resampling
  # boost pandas
  - numexpr
  - bottleneck
  - numba
  # end of boost pandas
  # ipywidgets
  - ipywidgets
  - ca-certificates
  - certifi
  - openssl
  # end of ipywidgets
  - nltk # natural language processing toolkit
  - pip 
  - pip:
    - python-dotenv # read .env file
    - pyalex # access OpenAlex API
    - metapub # query PubMed metadata
    - tqdm # progress bars
variables:
  PYTHONPATH: \automated_title_abstract_screening\src
  NUMEXPR_MAX_THREADS: 16

  # create an api key as described in 
  # https://ncbiinsights.ncbi.nlm.nih.gov/2017/11/02/new-api-keys-for-the-e-utilities/
  NCBI_API_KEY: YOURAPIKEY